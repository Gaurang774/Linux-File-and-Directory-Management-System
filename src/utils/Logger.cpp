#include "Logger.hpp"

// Logger is header-only; this file exists to satisfy the CMake source list
// and to serve as a future anchor for any non-inline implementation.

namespace filemgr {
    // intentionally empty — all implementation is in the header
} // namespace filemgr
